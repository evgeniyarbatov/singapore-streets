# Singapore Streets 🇸🇬

Exploring Singapore street names with OpenStreetMap.

## How Many Streets?

~4,443 streets based on the [Kaggle dataset](https://www.kaggle.com/datasets/evgenyarbatov/singapore-street-names).

## Documentation

Architecture and script reference: [docs/](docs/).

## Prerequisites

- Python 3
- [osmium](https://osmcode.org/osmium-tool/), `osmconvert`, and `wget` (for the OSM download/extract steps)
- [Ollama](https://ollama.com/) with a local model (default: `mistral-nemo:latest`) — only needed if you run `make categorize` without existing categories

## Setup

```bash
make install
```

## Full Pipeline

Run from the repo root:

```bash
# 1. Download regional OSM extract
make osm

# 2. Clip to Singapore boundary
make city

# 3. Extract street names and polylines from OSM
make streets

# 4. Normalize and filter street names
make clean

# 5. Group directional variants into a canonical name table (optional)
make canonical

# 6. Assign stable taxonomy categories (see below)
make categorize

# 7. Check category coverage
make category-report

# 8. Build the publishable dataset (CSV with name, category, polyline)
make dataset

# 9. Upload to Kaggle (optional)
make upload
```

Steps 3–8 (extract through dataset build) can be re-run together with:

```bash
make all
```

Use this whenever `extract_streets.py` or the cleaning scripts change, or after re-running `make city` with fresh OSM data. It skips `osm`, `city`, and `upload`, which you run separately.

To wipe generated outputs and rebuild from the existing OSM clip:

```bash
make fresh
```

To also re-download and re-clip OSM before rebuilding:

```bash
make fresh-all
```

`make reset` only deletes generated data (keeps `data/taxonomy.yaml`, `data/allowlist.txt`, and `data/categories-override.csv`).

## Categorization

Street categories use a fixed taxonomy defined in `data/taxonomy.yaml` — not open-ended LLM labels.

The pipeline applies categories in this order:

1. **Rules** — regex and lookup rules from the taxonomy (e.g. `Jalan`, `Bukit`, numbered avenues)
2. **LLM** — constrained JSON classification via `prompts/categorize-v1.md` (when running `make categorize`)
3. **Manual overrides** — `data/categories-override.csv` (tracked in git) wins over rules and LLM

Results are written to `data/street_categories.csv` (gitignored; regenerated by `make categorize`).

To fix a misclassified street, add a row to `data/categories-override.csv` with `street_name` and `category` (taxonomy id or display name), then run `make categorize` or `make dataset`.

Useful commands:

```bash
# Categorize streets (resumable; skips already-processed rows)
make categorize

# Print coverage stats and write data/category-stats.json
make category-report
```

## Tests

```bash
make test
```

## Story

I became interested in learning about Singapore street names after running long distances in Singapore. Then one day my son asked me how many streets there are in Singapore, and I decided to find out the answer. This turned out to be interesting as there is nothing obvious that defines a street name in OSM data. I think this is one of those projects where you can always make incremental improvements and discover more.
